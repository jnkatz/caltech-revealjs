name: Smoke Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  smoke-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      - name: Install R dependencies
        run: |
          Rscript -e 'repos <- Sys.getenv("RSPM", "https://cloud.r-project.org"); pkgs <- c("ggplot2", "knitr", "rmarkdown"); install.packages(pkgs, repos = repos); stopifnot(all(vapply(pkgs, requireNamespace, logical(1), quietly = TRUE)))'

      - name: Run smoke tests
        run: bash scripts/smoke-test.sh
